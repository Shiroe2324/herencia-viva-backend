export enum AskRecommendationStreamEvents {
  METADATA = 'metadata',
  CONTENT = 'content',
  ERROR = 'error',
  DONE = 'done',
}

export enum LLMChatRole {
  USER = 'user',
  SYSTEM = 'system',
  ASSISTANT = 'assistant',
}
